general:
  device: cpu # {cpu, cuda}
  logging-level: INFO
dataset:
  batch-size: 4
model:
  epochs: 100
  learning-rate: 0.00001
  type: bert
  bert:
    source: bert-base-uncased
encoder:
  type: bert
  bert:
    source: bert-base-uncased
    embedding-dimension: 768